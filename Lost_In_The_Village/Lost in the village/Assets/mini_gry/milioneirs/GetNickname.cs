using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class GetNickname : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public TextMeshProUGUI nicknameText;

    void OnEnable()
    {
        //var playerNickname: = PlayerPrefs.GetString("nickname");
       //// nicknameText.text = "Gracz: " + playerNickname;
       // nicknameText.enabled = true;

       // Debug.LogError("Player nickname" + playerNickname);

    }
}
